import 'package:flutter/material.dart';

const double kTop32Padding = 32.0;
const double kMediumPadding = 24.0;
const double kBigIconSize = 22.0;
const double kDefaultIconSize = 20.0;
const double kLTIconSize = 18.0;
const double kDefaultPadding = 16.0;
const double kItemPadding = 10.0;
const double kTopPadding = 8.0;
const double kMinPadding = 4.0;

const BorderRadius kMinBorderRadius = BorderRadius.all(Radius.circular(8.0));
const BorderRadius kSmallBorderRadius = BorderRadius.all(Radius.circular(10.0));
const BorderRadius kDefaultBorderRadius = BorderRadius.all(Radius.circular(14.00));
const BorderRadius kMediaBorderRadius = BorderRadius.all(Radius.circular(16.0));
const BorderRadius kBigBorderRadius = BorderRadius.all(Radius.circular(20.0));
const BorderRadius kMaxBorderRadius = BorderRadius.all(Radius.circular(24.0));

const String textSynopsis = 'Wreck-It Ralph wants to be loved by many people like his kind friend, Fix-It Felix. But no one likes evil characters like Ralph.';
const String textSynopsis2 = 'Ralph\'s goal was simple, wanting to win and get a medal to be considered a hero. But without realizing Ralph instead paved the way for criminals who can kill all the games in the game complex.';